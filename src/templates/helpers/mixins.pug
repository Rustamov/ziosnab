mixin icon(name,mod)
	if(mod)
		svg(class="svg-icon svg-icon-" + name + ' ' + mod)
			use(xlink:href="img/sprite/sprite/symbol/sprite.svg#" + name)
	else
		svg(class="svg-icon svg-icon-" + name)
			use(xlink:href="img/sprite/symbol/sprite.svg#" + name)
